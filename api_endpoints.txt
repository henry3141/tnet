Turtle_Needs:
    -getCommand -> Rust Tokio Server 
    -updateTurtle -> Rust Tokio Server
    -updateWorld -> Rust Tokio Server

Webinterface needs:
    -auth?
    -getWorld -> Rust Tokio Server
    -getTurtles -> Rust Tokio Server
    -getTurtle -> Rust Tokio Server
    -sendCommand -> Rust Tokio Server
    -cancelCommand -> Rust Tokio Server
    -updateTurtle -> Rust Tokio Server


Needed Endpoints:
    -getWorld() -> hashmap<(i32,i32,i32), Block>
    -getSets() -> hashmap<i32, Set>
    -getTurtle(set:i32,id:i32) -> Turtle
    -sendCommand(command:Command) -> i32
    -cancelCommand(id:i32) -> bool
    -updateTurtle(set:i32,id:i32) -> bool
    -getCommand(set:i32,id:i32) -> Command
    -updateWorld(pos:(i32,i32,i32),block:Block) -> bool


